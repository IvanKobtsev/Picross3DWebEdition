export enum EPage {
  Title,
  Puzzle,
}

export enum ETool {
  Block,
  Hammer,
  Brush,
  Color,
  Numbers,
}

export enum ELevel {
  Eight,
  Sun,
  Chamomile,
  Snowflake,
  Coffee,
}

export enum EAxis {
  XAxis,
  YAxis,
  ZAxis,
}

export enum ENumberType {
  normal,
  round,
  square,
}
